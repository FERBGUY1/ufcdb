require('dotenv').config();
// Tapology scraper — gym, coach, training partner data
// Run: node src/scrapers/tapology.js
async function main() { console.log('Tapology scraper ready — implementation in progress'); }
main().catch(console.error);
